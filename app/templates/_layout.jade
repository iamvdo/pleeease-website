doctype html
html

  block link
    -var selected = 'index'; //default

  head
    meta(charset='utf-8')
    meta(name='viewport',content='initial-scale=1')
    title Pleeease &middot; Postprocess CSS with ease
    meta(name='description',content='Write DRY, future-proof CSS and let Pleeease do the job')
    link(rel='stylesheet',href='http://fonts.googleapis.com/css?family=Damion')
    link(rel='stylesheet',href='http://fonts.googleapis.com/css?family=Open+Sans:400,700')
    if selected === 'index'
      link(rel='stylesheet',href='app.min.css')
      link(rel='icon', href='images/favicon.png')
    else
      link(rel='stylesheet',href='../app.min.css')
      link(rel='icon', href='../images/favicon.png')

    <!--[if IE]>
    style.
      .title { padding-top: 5em; }
    <![endif]-->

    script.
      document.createElement('header');
      document.createElement('nav');
      document.createElement('section');
      document.createElement('article');
      document.createElement('footer');

  body

    header

      .title
        h1
          a(href='/') pleeease
        p.subtitle Postprocess CSS with ease

      -var menu = { 'index': 'Get started', 'docs': 'Documentation', 'workflow': 'Improve your workflow'};
      -var docs = ['Configuration file', 'Features', 'CLI'];
      nav
        ul
          each val, key in menu
            if key === 'index'
              -var url='/';
            else
              -var url='/'+key;
            li
              if selected === key
                a.selected(href=url, title=val)= val
                if key === 'docs'
                  ul
                    each subval in docs
                      li
                        a(href='#'+subval.replace(' ', '-').toLowerCase())= subval

              else
                a(href=url, title=val)= val
          li
            a.play(href='/play') Interactive demo
          li.version Version <strong>0.4.x</strong>
          li.version <a href="http://twitter.com/pleeeaseCSS">@pleeeaseCSS</a>

    section

      article(class=selected)

        block content

        footer

          p Postprocess with <a href="/">Pleeease</a> by <a href="http://twitter.com/iamvdo">@iamvdo</a>.
          p.note Thanks to <a href="http://twitter.com/andreysitnik">@andreysitnik</a> for PostCSS and Autoprefixer, <a href="http://twitter.com/moox">@MoOx</a> for some PostCSS plugins and <a href="http://twitter.com/goetter">@goetter</a> for his contributions, feedback and testing.

    script.
      (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
      (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
      m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
      })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

      ga('create', 'UA-50460425-1', 'pleeease.io');
      ga('send', 'pageview');
