extends ../_layout

block link
  -var selected = 'playground';

block content

  .block.block--playground

      h2 Playground
      p Test the awesomeness!

      .textareas

        .textarea
          h3 Source
          p Write or paste code here (rem, unprefixed CSS, variables, etc.).
          textarea#input.
            div {
              display: flex;
              background: linear-gradient(red, blue);
            }
          p
            a(href='/#how-it-simply-works') See what Pleeease can do.

        .textarea
          h3 Result
          p
            strong See the result
            |  with 
            a(href='/docs') default options
          #output(class="textarea-area")

  script(src='pleeease.js')
  script(src='playground.js')